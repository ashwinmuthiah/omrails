<%= render 'pages/home' %>

<div id="pins">
  <%= render @pins %>
</div>

<div class="pagination pagination-centered">
	<%# Gives links to pages with rest of pictures %>
	<%= will_paginate @pins %>
</div>